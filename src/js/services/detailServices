app.service("detailsUtil", [function () {
    var event = {};

    this.setEvent = function (evnt) {
        event = evnt;

    };

    this.clearEvent = function () {
        event = {};
    };

    this.getEvent = function () {
        return event;
    };
}]);
